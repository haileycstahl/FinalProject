

#PCA : Data set has a lot of variables
#Use PCA on only numeric columns in data set
#data_num <- select_if(data, is.numeric) 
#data_num<-subset(data_num)
#Running PCA
#

#pcanum<- prcomp(na.omit(data_num))
#summary(pcanum)

#pcanum$rot[,1:50]